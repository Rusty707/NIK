{
  "name": "collectiveforge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "dev-deploy": "onchange '**/*.{html,css,js}' -- git add . && git commit -m 'Auto deploy' && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "gsap": "^3.13.0",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "onchange": "^7.1.0"
  }
}
